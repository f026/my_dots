local M = require("modules.declorations")
hl.monitor({
    output = M.MAIN_MONITOR,
    mode = "1920x1080@165",
    position = "0x0",
    scale = 1
})

-- hl.monitor({
--     output = M.SECOND_MONITOR,
--     disable = true
-- })
