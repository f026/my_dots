hl.cursor = {
    no_warps = true,                    -- отключает warp в большинстве случаев
    warp_on_change_workspace = 0,       -- отключает warp при смене WS
    persistent_warps = false,           -- курсор в центр окна при фокусе
    inactive_timeout = 0                -- курсор никогда не скрывается
}
