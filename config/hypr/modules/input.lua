hl.config({
    input = {
        kb_layout = "us,ru",              -- Раскладки через запятую
        kb_variant = ",",                 -- Варианты для каждой раскладки
        kb_options = "grp:alt_shift_toggle",  -- Комбинация для переключения
        repeat_rate = 30,
        repeat_delay = 400,
    }
})
